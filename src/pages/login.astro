---
import Layout from "../layouts/Layout.astro";
import Topbar from "../components/Topbar.astro";
import Footer from "../components/Footer.astro";
---

<Layout title="Iniciar Sesión - Ama el proceso">
    <div class="page-wrapper">
        <Topbar />

        <main class="login-container" style="margin-top: -4em;">
            <div class="login-card fade-in">
                <h1>Bienvenida de nuevo</h1>
                <p>Accede a tu espacio personal para continuar tu proceso.</p>

                <div class="auth-buttons">
                    <button id="google-login-btn" class="google-btn">
                        <div class="icon-wrapper">
                            <svg class="google-icon" viewBox="0 0 24 24">
                                <path
                                    d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"
                                    fill="#4285F4"></path>
                                <path
                                    d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"
                                    fill="#34A853"></path>
                                <path
                                    d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"
                                    fill="#FBBC05"></path>
                                <path
                                    d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"
                                    fill="#EA4335"></path>
                            </svg>
                        </div>
                        <span>Continuar con Google</span>
                    </button>

                    <p class="gdpr-warning">
                        Al continuar, aceptas nuestra <a
                            href="/privacidad"
                            target="_blank"
                            rel="noopener noreferrer">Política de Privacidad</a
                        > y <a
                            href="/aviso-legal"
                            target="_blank"
                            rel="noopener noreferrer">Términos de Uso</a
                        >. Tus datos serán tratados de forma segura conforme al
                        RGPD.
                    </p>
                </div>
            </div>
        </main>
    </div>
</Layout>

<script>
    import { auth, loginWithGoogle } from "../firebase/client";
    import { onAuthStateChanged } from "firebase/auth";

    const googleBtn = document.getElementById("google-login-btn");

    // Check if already logged in
    onAuthStateChanged(auth, (user) => {
        if (user) {
            window.location.href = "/panel";
        }
    });

    googleBtn?.addEventListener("click", async () => {
        try {
            await loginWithGoogle();
        } catch (error) {
            console.error("Google Login failed:", error);
            alert("Hubo un error al iniciar sesión con Google.");
        }
    });
</script>

<style>
    /* Ensure no scroll on the page */
    :global(body) {
        overflow: hidden;
        margin: 0;
        padding: 0;
        height: 100vh;
        height: 100svh;
    }

    .page-wrapper {
        display: flex;
        flex-direction: column;
        height: 100vh;
        height: 100svh;
        background-color: var(--green); /* Brand background color */
        overflow: hidden;
    }

    /* Adjust Topbar positioning if needed, or let it flow naturally */
    :global(header) {
        flex-shrink: 0;
    }

    .login-container {
        flex: 1;
        display: flex;
        justify-content: center;
        align-items: center;
        padding: 1rem;
        overflow: hidden; /* Prevent internal scrolling */
        margin-top: -4em; /* User adjustment */
    }

    .login-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        padding: 3rem;
        padding-top: 1rem;
        border-radius: 24px;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 500px;
        min-height: 10vh;
        width: 100%;
        border: 2px solid var(--ink); /* Brand border style */
    }

    h1 {
        font-family: "Gulf Display", sans-serif; /* Brand font */
        font-size: 2.5rem;
        color: var(--ink);
        margin-bottom: 0.5rem;
        font-weight: 400;
    }

    p {
        color: var(--ink);
        margin-bottom: 2rem;
        line-height: 1.6;
        font-family: "Inter", sans-serif;
    }

    .auth-buttons {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .google-btn {
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center; /* Center the text */
        width: 100%;
        padding: 12px 24px;
        background: white;
        border: 2px solid var(--ink);
        border-radius: 999px;
        font-family: "Inter", sans-serif;
        font-weight: 600;
        color: var(--ink);
        cursor: pointer;
        transition: all 0.2s ease;
        font-size: 1rem;
        box-shadow: 2px 3px 0px 0px #000000;
    }

    .icon-wrapper {
        position: absolute;
        left: 24px;
        display: flex;
        align-items: center;
    }

    .google-btn:hover {
        transform: translateY(-1px);
        box-shadow: 4px 5px 0px 0px #000000;
    }

    .google-btn:active {
        transform: translateY(0);
        box-shadow: 2px 3px 0px 0px #000000;
    }

    .google-icon {
        width: 24px;
        height: 24px;
    }

    .gdpr-warning {
        font-size: 0.8rem;
        color: #666;
        margin-top: 1rem;
        margin-bottom: 0;
        text-align: left;
        line-height: 1.4;
    }

    .gdpr-warning a {
        color: var(--ink);
        text-decoration: underline;
        font-weight: 600;
    }

    .fade-in {
        animation: fadeIn 0.8s ease-out;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
